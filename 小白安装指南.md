# 逻敏 · Academy 辩论教练 —— 小白安装指南

> 给完全没装过软件的同学 / 长辈看的。每一步都写清楚「点哪里」，照着做就行。
> 遇到看不懂的词，直接跳到最后「❓ 常见问题」找。

---

## 🕐 先花 30 秒搞清楚三件事

| 你想知道 | 答案 |
|---|---|
| **这是什么？** | 装在你自己电脑里的一个「AI 辩论教练」窗口。可以帮你**备赛**（立论 / 质询 / 攻防）、**复盘**比赛文字稿、当**评判**打分写述票，也能随便问辩论问题。 |
| **要花多久？** | **安装约 1~3 分钟**（全自动，你只需等）。另外要提前准备一个 API Key，约 5 分钟（见第 ① 步）。 |
| **要不要装别的？** | **什么都不用装。** 不用装 Python、不用装 Node、不用装浏览器插件。软件自带运行环境，双击就能用。只需要：一台 Windows 10 / 11 的电脑 + 能上网。 |

> ⚠️ 唯一要你先准备的东西：**一个 API Key（一串密钥）**。
> 它就像「AI 充值卡号」，软件靠它连上 AI 大脑。**没有 Key 就暂时用不了。**
> 不知道怎么弄？看下面第 ① 步，或者**找队里用过 AI 的队友帮忙，5 分钟就好**。

---

## ① 安装前准备：拿到一个 API Key（就这一件事）

### 1-1 推荐用「DeepSeek 官方」

| 项目 | 内容 |
|---|---|
| 网址 | <https://platform.deepseek.com>（国内能直接打开） |
| 费用 | 按用量扣费，很便宜，**先充几块钱**就够用很久 |
| 推荐模型名 | `deepseek-v4-flash`（后面要填这个） |

### 1-2 拿 Key 的四小步

1. 打开上面的网址，用**手机号注册并登录**。
2. 在左侧菜单找到 **「API Keys」**，点 **「创建 API Key」**。
3. 屏幕会显示一串 `sk-` 开头的密钥 —— **它只显示这一次！**
   请立刻**复制**它，并**粘贴到桌面新建的记事本里**存着（这步很重要，别偷懒）。
4. 在同一个平台里**充值几块钱**（不充钱的话后面会提示连不上）。

> 💡 如果你已经有别家的 Key（硅基流动 / Moonshot / 智谱 / OpenAI / 其他兼容接口），
> 也可以直接用，不用再注册 DeepSeek。填法见第 ④ 步。
>
> 🔒 **放心**：这串 Key 只存在**你自己的电脑里**，不会上传到任何别的地方。

---

## ② 方式一：一键安装 EXE（⭐ 推荐给小白）

**适合：绝大多数人。全程自动，装完桌面会出现一个图标，以后双击图标就能用。**

### 第 1 步：找到文件

找到别人发给你的这个文件：

```
Academy辩论教练-安装版.exe
```

> 建议在**「下载」文件夹**里找，或者问发你文件的人放在哪了。

### 第 2 步：双击它

用鼠标**双击**这个文件。

### 第 3 步：如果弹出蓝色警告框（很多人会碰上）

如果屏幕中间出现一个蓝色窗口，写着 **「Windows 已保护你的电脑」**：

> 👉 点 **「更多信息」**（字很小，在提示文字下面）
> 👉 然后点 **「仍要运行」**

**这不是病毒。** 只是这个软件没花钱买微软的「签名证书」，Windows 见到没签名的新程序都会这么问一句。详细说明见 FAQ 第 2 条。

### 第 4 步：等着（喝口水）

会弹出一个中文安装窗口：**「Academy 辩论教练 安装向导」**，下面有一条来回走的进度条。

这时**什么都别点、别关窗口**，等它自己走完，约 **1~3 分钟**。

| 你在安装窗口上会看到 | 说明 |
|---|---|
| 正在解压安装包… | 在拆包裹 |
| 正在解压程序文件… | 在把几百个文件铺开 |
| 正在安装程序文件… | 复制到安装目录 + 建快捷方式 |
| 正在启动… | 快好了 |

> ⏳ **慢是正常的**：里面有几百 MB 文件要复制，电脑慢的话可能要 3 分钟。
> 超过 5 分钟还卡着不动，再看 FAQ 第 3 条。

### 第 5 步：装好了

会弹出一个框写着 **「安装完成！」**，同时**软件窗口会自动打开**。点「确定」即可。

看看你的桌面 —— 是不是多了一个图标：**「Academy 辩论教练」**？🎉 有了它就装好了。

### 第 6 步：以后怎么用

**以后永远只需要：双击桌面上的「Academy 辩论教练」图标。**

也可以：点屏幕左下的 **开始** → 找到 **「Academy辩论教练」** 文件夹 → 点里面的图标。

> ✅ 装完后，那个 `.exe` 安装包就可以**删掉了**，留着也没用（想给同学装可以留着）。

---

## ③ 方式二：便携版 ZIP（EXE 装不上时的备选）

**适合：杀毒软件把 EXE 拦死了、或你不想往电脑里装东西的情况。**

### 第 1 步：解压

找到文件 `Academy-Bianlun-Coach-portable.zip`，在它上面点**鼠标右键** → 选 **「全部解压缩…」** → 直接点 **「提取」**（不用改位置）。

会解压出一个同名文件夹并自动打开。

> 📌 建议解压到**桌面或 D 盘的普通文件夹**里，**不要**放在 C 盘的系统目录。

### 第 2 步：双击 `start.bat`

打开这个文件夹，找到 **`start.bat`**，**双击**它。

第一次会有一个黑色窗口一闪而过（不用管它），稍等几秒，就会弹出软件窗口。

### 第 3 步：以后怎么用

**以后每次都去这个文件夹，双击 `start.bat`。**

嫌麻烦？可以在 `start.bat` 上点右键 → **「显示更多选项」**（Win11）→ **「发送到」** → **「桌面快捷方式」**，桌面就有快捷图标了。

> ⚠️ **注意**：便携版没有自动卸载，不用了把整个文件夹**删掉**即可（删之前记得先备份数据，见第 ⑥ 步）。

---

## ④ 第一次打开：配置 API（必做）

软件第一次打开时，需要你把第 ① 步拿到的 Key 填进去。**填一次就行，以后自动记住。**

### 按这三步做

| 步骤 | 你要做什么 |
|---|---|
| **1. 打开设置** | 点软件窗口**右上角**的 **「⚙️ 设置」** 按钮 |
| **2. 选服务商** | 在「服务商」这一排小卡片里，点 **「DeepSeek 官方」**（右上角有个「推荐」小标签） |
| **3. 填 Key** | 在「API Key」那个框里，**右键 → 粘贴**你之前存的那串 `sk-` 开头的密钥 |

### 然后填模型名

在「模型」这一栏填：

```
deepseek-v4-flash
```

> 不确定填什么？可以先点右边的 **「获取模型」**，让软件自动列出这家服务商有哪些模型，从下拉框里挑一个就行。

### 最后保存

点 **「保存配置」**。

👉 建议先点一下 **「🔌 测试连接」** —— 它会告诉你「能不能连上」，省得后面干等。

看到「保存成功」之类的提示，就搞定了！**关掉设置窗口，开始用吧。**

### 想换别的服务商？

| 服务商 | 说明 |
|---|---|
| **DeepSeek 官方** | ⭐ 推荐，国内直连，便宜 |
| 硅基流动 | 国内平台，模型多 |
| Moonshot Kimi | 月之暗面 |
| 智谱 GLM | 清华系 |
| OpenAI | 需要能访问其服务的网络环境 |
| ＋ 自定义接口 | 你自己的 OpenAI 兼容地址，需要自己填 API 地址 |

> 💡 软件支持**保存多套配置**（比如「DeepSeek 主力」「硅基流动备用」），随时切换。

---

## ⑤ 基本用法（30 秒看完）

| 想做什么 | 怎么做 |
|---|---|
| **选模式** | 点**左侧**的按钮：**💬 自由问答 / 📋 备赛 / 🔍 复盘 / ⚖️ 评判**（四种模式的对话互相独立，互不干扰） |
| **发消息** | 在下面的输入框打字，按 **`Ctrl + Enter`** 发送（也可以点发送按钮） |
| **看长交付** | 让教练写「备赛包 / 复盘报告」这类长篇时，它会存成文件；点顶部 **「📦 产物」** 可以预览，还能**转 Word / Excel / PDF** 或导出到任意文件夹 |

> ⏳ 发出去之后要**等几秒到几分钟**（复杂任务更久），回答会像打字一样一个字一个字冒出来，这是正常的。

---

## ⑥ 怎么卸载 / 数据会不会丢（重要，请放心看）

### 卸载方法（任选一种）

- **方法一**：点 **开始** → 找到 **「Academy辩论教练」** 文件夹 → 点里面的**卸载**入口
- **方法二**：打开安装目录，双击里面的 **`uninstall.bat`**
  （安装目录是：`C:\Users\你的用户名\AppData\Local\AcademyDebateCoach`）

### 🔐 卸载时数据会自动备份 —— 不会丢！

卸载程序**会先把你的数据打包好放到桌面**，然后才删除程序。

备份位置（桌面上会自动新建这个文件夹）：

```
桌面 \ Academy辩论教练-数据备份 \ Academy数据_年月日_时间.zip
```

里面包含：**对话记录、API Key、个人资料库**。

> 如果备份没成功生成，卸载程序会**自动停下来、不删任何东西**，并提示你手动复制。所以不会「悄悄删光」。

### 重装后怎么恢复

1. 重新装好软件。
2. 打开备份文件夹，解压那个 `.zip`，得到一个 `data` 文件夹。
3. 把这个 `data` 文件夹**整个放回安装目录**（就是上面那个 `AcademyDebateCoach` 文件夹）里，覆盖同名文件夹。
4. 重新打开软件 —— 你的对话记录和 Key 就都回来了。

---

## ❓ 常见问题 FAQ

### 1️⃣ 杀毒软件报警 / 说有病毒，是不是中毒了？

**不是。这是一个「误报」，几乎所有自己打包的小软件都会遇到。**

原因：这个安装包是一个**自解压文件** —— 它会先把自己肚子里的几百个文件释放出来再安装。
而病毒、木马干的事**一模一样**（也是把自己藏起来再释放文件），所以杀毒软件看到这种行为就先拦下来再说。
**它不认识这个软件，就当可疑处理了。**

**怎么办（任选一种）：**

| 做法 | 怎么操作 |
|---|---|
| **点「允许 / 信任 / 保留」** | 杀软弹出提示时，一般有小字的「更多信息」「允许此文件」「信任」，点它 |
| **加白名单** | 打开杀毒软件 → 找「信任区 / 白名单 / 排除项」→ 把这个 exe（或下面第 9 条里的安装目录）加进去 → 重新双击 |
| **用便携版** | 实在不放心，就别用 EXE，改用第 ③ 步的**便携版 ZIP** —— 解压后双击 `start.bat`，不运行 exe，杀软基本不拦 |

> 🛡️ 补充说明：本软件是**开源免费软件**（CC BY-NC-SA 4.0 许可），本地运行，不含任何广告或捆绑安装。

### 2️⃣ 出现蓝色框「Windows 已保护你的电脑」（SmartScreen）

这是 Windows 自带的保护提示，**不是报错**。

> 👉 点 **「更多信息」** → 点 **「仍要运行」**

**为什么会出现？** 因为微软的「签名证书」一年要花不少钱，小工具一般没买。
Windows 对**没有签名的新程序**一律先问一句，很多个人开发的小软件都这样。

**安全吗？** 只要你这个文件是**认识的人发给你的**（不是网上随便下的），点「仍要运行」是安全的。

### 3️⃣ 双击 EXE 没反应 / 进度条卡住不动了

按顺序试：

1. **先等 1 分钟。** 安装程序要先拆开几百 MB 的文件，一开始可能看着「没动静」，其实在忙。
2. **看看是不是已经装过了。** 如果再次双击时提示「安装程序已经在运行，请等待当前安装完成」，说明第一次那次还在跑，等着就行。
3. **关掉杀毒软件再试一次。** 杀软在后台扫描会拖慢甚至打断安装（见第 1 条）。
4. **看桌面有没有新图标。** 有的话说明其实装好了，直接双击图标用。
5. **确认文件完整。** 安装包应该有 **200 MB 左右**；如果只有几 MB，说明**下载没下完**，请重新要一份。
6. **还是不行 → 换便携版。** 用第 ③ 步的 ZIP 方式，一样能用。

### 4️⃣ 提示「缺少 API Key」/「还没配置 API Key」

说明 Key 还没填进去（或者填了没保存）。

👉 点右上角 **「⚙️ 设置」** → 把 `sk-` 开头的 Key 粘进「API Key」框 → 点 **「保存配置」**。

> 详细步骤见本文第 ④ 步。
> **没有 Key 就真的用不了**，需要去服务商官网注册获取（见第 ① 步）。

### 5️⃣ Key 填了，但连不上 / 一直转圈 / 报错

先点设置里的 **「🔌 测试连接」**，它会告诉你卡在哪。常见原因三种：

| 可能原因 | 怎么排查 |
|---|---|
| **① 网络问题** | 确认电脑能正常上网；试试关掉 VPN / 代理软件再连一次；公司 / 校园网有时会拦境外接口 |
| **② Key 填错了** | Key 很长，容易少复制一个字符或多带空格。回到平台重新复制一次、完整粘贴（**前后都不要有空格**）；也可以点 Key 框右边的 👁 眼睛按钮把它显示出来核对 |
| **③ 余额不足** | 去服务商官网看看账户余额，**充几块钱**再试。没充钱是最常见的「连不上」原因 |

> 如果用的是「自定义接口」，还要检查 **API 地址**填得对不对（DeepSeek 官方是 `https://api.deepseek.com`）。
> 另外**模型名**填错也会失败，不确定就点 **「获取模型」** 从列表里选一个。

### 6️⃣ 安装要多久？占多大空间？

| 项目 | 大概数值 |
|---|---|
| 安装耗时 | **1~3 分钟**（电脑慢的话可能到 5 分钟） |
| 安装包大小 | 约 200 MB |
| **装完占用空间** | **约 550 MB** |
| 安装过程临时需要 | 约 900 MB（先解压再复制，装完临时文件会自动删掉） |

> 💾 所以 **C 盘至少留出 1 GB 空闲**再装，比较稳妥。

### 7️⃣ 能装到 D 盘吗？能自己选位置吗？

**目前不能。** 安装版会**自动固定装到系统用户目录**里，安装时没有「选择位置」这一步 —— 这是为了让你不用做多余的选择，点一下就装好。

装好后的位置是：

```
C:\Users\你的用户名\AppData\Local\AcademyDebateCoach
```

> 💡 **想放 D 盘的话**：请用第 ③ 步的**便携版 ZIP** —— 它可以解压到**任意目录**（比如 `D:\辩论教练`），双击 `start.bat` 就能用。

### 8️⃣ 我的对话记录存在哪？会不会泄露？

**只存在你自己的电脑里，不会泄露。** 这是这个软件最重要的设计。

| 数据 | 存在哪 |
|---|---|
| 对话记录、API Key、个人资料库 | 你电脑本地的 `AcademyDebateCoach\data` 文件夹 |
| 长交付文件（备赛包等） | 本地 `data\deliverables` 文件夹 |

- ✅ **不上传**：对话内容只发往你在设置里填的那个 AI 服务商地址，除此之外不发给任何人。
- ✅ **Key 不外传**：API Key 存在本机，只在调用 AI 时发给你自己填的那个地址。
- ✅ **只有「问 AI」才联网**：软件本身是本地运行的。

### 9️⃣ 怎么卸载？卸载后我的记录还在吗？

**卸载方法**：开始菜单 → **Academy辩论教练** → 卸载入口；
或者打开安装目录，双击 **`uninstall.bat`**。

**卸载后数据还在吗？在！**

卸载时会自动把你的**对话记录、API Key、个人资料库**打包成一个带日期时间的 `.zip`，放在：

```
桌面 \ Academy辩论教练-数据备份
```

**重装后恢复**：把备份 zip 解压出来的 `data` 文件夹，整个放回安装目录即可（详见第 ⑥ 步）。

> ⚠️ 只有一点要注意：**别手动去删** `AcademyDebateCoach` 文件夹当卸载 —— 那样**不会自动备份**，数据就真没了。
> 请一定走 `uninstall.bat` 或开始菜单的卸载入口。

### 🔟 装完桌面没有图标，怎么办？

别急，程序其实已经装好了，只是快捷方式没建成功（不影响使用）。

**方法一**：直接打开安装目录，双击里面的 **`launch.vbs`**，软件就启动了。

**方法二**：自己建一个快捷方式 ——
在 `launch.vbs` 上点**右键** → **「显示更多选项」**（Win11）→ **「发送到」** → **「桌面快捷方式」**。

### 1️⃣1️⃣ 换台电脑 / 重装系统，还能用吗？

**能。**

- **方式一（最省事）**：在新电脑上重新走一遍第 ② 步安装，然后把旧电脑备份的那个 `data` 文件夹拷过去（见第 9 条），记录和 Key 都回来了。
- **方式二**：把整个程序文件夹拷贝到 U 盘 / 新电脑，在新位置双击 `start.bat` 即可。

### 1️⃣2️⃣ 打开软件慢 / 回答很慢，正常吗？

- **打开慢**：第一次启动要加载运行环境，等几秒到十几秒是正常的。
- **回答慢**：复杂任务（比如写完整备赛包）本来就要几分钟，回答会一个字一个字往外冒，请耐心等。
- 如果**超过 10 分钟**还一点动静都没有，可以点「停止」再发一次；还是不行就按第 5 条检查网络和余额。

---

## 📞 还是搞不定？

1. 先看看上面的 FAQ，90% 的问题都在里面。
2. 安装遇到问题：把安装窗口上的**提示文字拍个照**，发给给你文件的人。
3. 使用遇到问题：看软件同目录下的 `使用说明.txt`。

---

## 📋 一页速查表（可以打印出来照着做）

| 步骤 | 动作 | 大概耗时 |
|---|---|---|
| 1 | 去 <https://platform.deepseek.com> 注册 → 创建 API Key → 复制存好 → 充几块钱 | 5 分钟 |
| 2 | 双击 `Academy辩论教练-安装版.exe` | — |
| 3 | 蓝色框出现就点「更多信息」→「仍要运行」 | 5 秒 |
| 4 | 等安装向导走完（不要关窗口） | 1~3 分钟 |
| 5 | 桌面出现「Academy 辩论教练」图标 → 装好了 🎉 | — |
| 6 | 打开软件 → 右上角「⚙️ 设置」→ 选「DeepSeek 官方」→ 粘 Key → 模型填 `deepseek-v4-flash` → 保存 | 1 分钟 |
| 7 | 左侧选模式 → 打字 → `Ctrl + Enter` 发送 → 开始用 | — |

---

<sub>本软件为开源免费软件，采用 CC BY-NC-SA 4.0 许可（署名 · 非商业性使用 · 相同方式共享）。
系统要求：Windows 10 / 11（64 位）、能上网、至少 1 GB 空闲空间。</sub>
